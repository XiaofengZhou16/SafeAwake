# Contributing to SafeAwake

感谢你帮助改进 SafeAwake。

## 开发流程

1. 先搜索现有 Issue，避免重复工作。
2. 对行为变化较大的功能，先创建 Feature Request 讨论安全边界。
3. 从 `main` 创建短分支。
4. 提交代码、测试和必要的文档更新。
5. 运行 `swift test`，然后创建 Pull Request。

## 不可破坏的安全边界

以下变化不会被接受：

- 绕过 MacBook 的物理合盖睡眠。
- 使用管理员权限或持久修改 `pmset` 作为常规工作方式。
- 隐藏当前防休眠状态。
- 默认在电池供电下无限期运行。
- 在 App 退出后留下持久防休眠设置或后台进程。

## 代码风格

- 优先使用 Apple 公开 API。
- 将状态逻辑放在 `SafeAwakeCore` 中，并为新分支增加测试。
- UI 文案应明确区分“显示器关闭”和“物理合盖”。
- 不收集遥测、使用记录或个人信息。
